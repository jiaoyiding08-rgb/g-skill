# G Skill 0.1 从这里开始

## 给阿辉的使用顺序

第一步，解压开发包。把整个 `gskill-v0.1.0` 文件夹交给 Codex。先让它读 `README.md` 和 `AGENTS.md`，运行离线 demo。暂时不要让它重新发明架构、接支付或设计排行榜。

第二步，只安装 Ground、Real、Ship、Review、Repeat。Lab 留给你维护，避免普通成员把改写正式规则当成日常功能。

第三步，拿自己的十个真实任务试用。记录每次给出的下一步是否可做、是否真的执行、哪里卡住。向外部模型发送材料前先去掉姓名、联系方式、客户资料和隐私。得到成员明确同意后，才收集他们的样本。

第四步，再讨论接什么工具。先确认方法有用，再增加自动记录、外部核验和优化器。

## 你现在实际拿到的东西

六个 Skill 都写成独立文件。每个都有触发条件、排除情况、输入、决策、步骤、完成标准、证据、停止、交接、写入、安全、评测和来源说明。

它们的共同输出分成两层。

**成员看到行动卡。** 包含当前结论、一个主要动作、完成标准、核对材料和阻塞项。默认不把长 JSON 发给成员。

**系统读取结构化记录。** JSON 区分任务、产物、实际结果、事前预测、候选规则和重复实例。生成一份记录只算建议。真实写入和完成状态由工具返回。

## 一条具体演示

运行 `python -m kernel demo --out .local/demo-01`。

演示创建三次相互独立的模拟任务。每次先保存目标和完成标准，再记录行动前判断。没有产物时检查结果是 `WAITING_EVIDENCE`。创建文件、登记哈希后，本地核验可以通过。

这里通过的条件很窄：文件存在，且当前内容与登记版本相同。演示没有证明说明写得好，也没有证明产品有人买。

三次记录生成一个候选经验和一个重复任务簇。它们仍是模拟材料。Lab 看到没有真实模型评估和独立留出证据，会保持 `CONTINUE_EXPERIMENT`，不会升级正式 Skill。

## 六个模块怎样配合

| 你当前的话 | 使用模块 | 本轮合理终点 |
|---|---|---|
| 想做，但不知道先做哪一步 | Ground | 一张能开始的行动卡 |
| 我觉得这个东西有人需要 | Real | 一个能提供信息的小测试 |
| 还想再改十次，要不要发 | Ship | 可交付、一次必要修改、暂停或结束 |
| 做完了，结果和预期不同 | Review | 基于已有记录的对照和候选规则 |
| 这件事又做了一遍 | Repeat | 有独立实例支持的模板候选 |
| 这个 Skill 连续判断错了 | Lab | 可复现的失败与待批准的修改方案 |

每次按需要选一个。用户只要一份文案，就先给文案。学习、休息和放弃也可以是合理选择。

## 第一版主动收住的范围

没有持续录屏，没有自动操控电脑，没有抖音商品上架，没有真实支付核验，没有奖金排名，也没有自动改写正式版。代码是单用户本地参考实现，不能直接作为多人 SaaS 上线。

当前最重要的验证问题只有一个：**在同样的任务、时间和工具条件下，这些 Skill 能否让下一步更清楚、更容易执行。** 这个答案还需要真实试用。

## 发给 Codex 的启动任务

```text
打开这个 G Skill 项目，先读 README.md、AGENTS.md 和 docs/acceptance.md。
按现有说明运行单元测试、项目检查和离线 demo。
不要改写六个模块的定位，不要连接支付、社交平台或自动观察电脑。
报告实际通过与失败的项目，并说明哪些结果仅来自合成案例。
随后给出最小真实试用方案。没有我的确认，不调用收费模型、不上传材料、不覆盖正式 Skill。
```
