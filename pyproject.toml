[project]
name = "gskill-reference"
version = "0.1.0"
description = "G Skill prompt-native prototype and local verification reference runtime"
requires-python = ">=3.10"
dependencies = ["jsonschema[format]>=4.23,<5"]
license = "Apache-2.0"

[project.optional-dependencies]
dev = ["PyYAML>=6,<7"]
