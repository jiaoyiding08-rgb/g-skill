# 数据和隐私

默认只使用用户主动提供或明确授权的材料。没有持续录屏、麦克风监听、浏览器观察和自动读取聊天记录。不会主动连接云端。

SQLite 为本地明文。POSIX 下创建数据库后设为 0600，但这不提供磁盘加密、账号隔离或管理员防护。把项目放在自己的受保护目录，不放公共共享盘。

导出文件可能包含私人目标、时间、文件路径、证据、判断和候选经验。`private_export` 标记是提示，不会自动去标识。公开展示前应另行挑选并脱敏，不能把整个导出直接发群里。

删除命令需要 `--confirm`，删除所选运行和相关派生记录。不会删除用户原始产物，也不会删除外部备份和此前导出。SQLite 开启 secure_delete 只是部分措施，不承诺磁盘、快照或同步服务上的取证级擦除。

真实模型评估只有设置 `G_ALLOW_NETWORK=1` 后才能调用。模型会收到选定 Skill、共同约定、响应 Schema，以及该轮案例的 input/context。不会自动发送数据库或全部历史。端点和模型必须显式设置；应先阅读服务方的数据规则、确认预算和参与者授权。

脚本不记录 API 密钥，错误信息避免回显请求正文、密钥和服务端错误体。结果报告仍会包含模型输出，输出有可能复述输入，请当作私有材料保存。

用于测试的本包案例全部为合成材料。真实失败案例加入公共仓库前，需要单独同意与去标识。社群奖金、身份排名与私人行为记录的隔离仍需单独设计，本版没有上线这些功能。
