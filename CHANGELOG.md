# 变更记录

## 0.1.0-alpha · 2026-09-18

公开发布整理：采用 Apache-2.0，添加英文简介；补全 jsonschema[format] 依赖以启用日期校验；排除原包 reports 运行产物及旧 MANIFEST.json（其哈希对应未整理的原压缩包）。保留源材料指纹 docs/source-manifest.json，业务代码和六个 Skill 保持原包内容。

依据设计文档制作六个独立 Skill、共同规则、统一 Schema、SQLite 本地参考运行、CLI、交接/发布检查、72 个合成案例和可选评估适配。

对原文的实现性修订：

- Ground 完成一张行动卡，与用户完成现实任务分开。
- Real 的实验完成和假设支持分开。测试结果缺失时保留未知。
- Ship 的时间预算结束后重新核验硬风险与核心价值，不能到点强行发布。
- Review 缺少事前快照时使用 RETROSPECTIVE_ONLY；新经验从候选规则开始。
- Repeat 依赖独立 episode 与原始 run 的对应关系。重复编辑去重。R3/R4 不在参考存储里直接启用。
- Lab 保留原 skill-improver 已有的行为契约、基线、留出、授权和回退机制。新增工程校验不被描述为对原版从零补齐。
- E1 文件哈希只证明文件存在与版本一致。其他核验器尚未实现。
- 模型输出只能提议写入，无法直接写成功状态。
- 程序测试、模型效果评估和真实用户试用分别标注。
- provider 只发送选定模块的响应 Schema，不发送无关测试结构与评分规则。

项目状态仍为本地 alpha。没有调用收费模型、部署网站、上传私密记录或发布正式 Skill。
